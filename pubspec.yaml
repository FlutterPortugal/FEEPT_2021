name: feep_competition2021
description: Flutter Engage Extended (Portugal) - Competition 2021

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  logger: ^1.0.0
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  http: ^0.13.1
  intl: ^0.17.0
  provider: ^5.0.0
  location: ^4.1.1
  supercharged: ^2.0.0
  flutter_weather_icons: ^1.0.2
  smooth_page_indicator: ^0.2.0
  simple_animations: ^3.0.3
  

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
